# Web Build and GitHub Pages

NeoDex uses Vite for development and production builds.

Commands:

```bash
npm install
npm run dev
npm test
npm run build
```

The GitHub Actions workflow builds `dist/` and deploys it to GitHub Pages.
